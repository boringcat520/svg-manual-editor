# 更新说明

## 0.1.37 — 2026-08-28

兼容 Codex / Inkscape 分层 SVG：画板白底不再被拖走，分区框会带上框内卡片。

### 修复

- **拖动白色背景导致整图错位。** Codex 生成的图常把铺满画布的 `artboard-bg` 放在 `layer-background`，卡片放在另一层 `layer-components`。编辑器曾把带 `data-name` 的整层当成一个组件，拖白底等于拖走所有分区框和标题，卡片留在原地。现在画板矩形不可选；`data-layer` / `layer-*` 图层不再当组件。
- **拖动分区大框时，浅层卡片不跟着动。** 「参与方节点」等 `data-object="section"` 大框与内部卡片不是父子节点。拖动分区框时，会带上几何上落在框内的卡片及其文字。
- **置顶 / 置底把文字从 `<g>` 里抽到顶层。** 现在移动的是整个组件组，不再拆散卡片。

## 0.1.36 — 2026-08-28

保留 `<g>` 组件归属，并停止把画布尺寸写成 `NaN`。

### 修复

- **保存后根节点出现 `width="NaN"` `height="NaN"`。** 普通保存也会走导出尺寸逻辑；`NaN <= 0` 在 JavaScript 里为假，非法宽高被写进文件。保存时只写有限数值；坏掉的宽高回退到 `viewBox`（例如 `1800×1080`）。
- **同组矩形和文字不能一起选中、移动、缩放。** SVG 不允许 `<text>` 作为 `<rect>` 的子节点，正确结构是二者作为兄弟放在同一个 `<g>` 里。编辑器以前按单个元素操作，文字看起来像被提到单独一层。现在同一组件组（`data-object`，或组内同时有图形和文字）会一起选中、移动、缩放、排序和删除；也认 `data-owner` / `data-parent-shape`。

## 0.1.35 — 2026-08-27

- 属性栏在窄窗口（例如右侧开着 Chat）时不再掉到画布下方，始终停在右侧。
- 顶栏扩展图标放大到 36px。
